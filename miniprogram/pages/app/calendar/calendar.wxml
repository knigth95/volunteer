<cu-custom bgColor="bg-gradual-blue" isBack="{{true}}">
    <view slot="backText">返回</view>
    <view slot="content">校历</view>
</cu-custom>

<view wx:for="{{url}}" wx:key="index">
    <image style="width: 100%;" src="{{item}}" bindtap="previewCalendar"></image>
</view>


